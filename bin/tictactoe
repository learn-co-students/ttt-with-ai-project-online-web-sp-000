#!/usr/bin/env ruby

require_relative '../config/environment'


puts "Hi, welcome to Tic Tac Toe!" 
puts "Which game mode would you like to play?
      1 - You v. a Friend
      2- You v. Computer
      3- Computer v. Computer"

  mode_chosen = gets.strip

  if mode_chosen == "1"
      game = Game.new(Players::Human.new("X"), Players::Human.new("0"), Board.new)
      game.play
    end
  
  if mode_chosen == "2"
    puts "Do you want to go first? y/n"
    input = gets.strip
    if input == "y"
      game = Game.new(Players::Human.new("X"), Players::Computer.new("0"), Board.new)
      game.play
    elsif input == "n"
      game = Game.new(Players::Computer.new("X"), Players::Human.new("O"), Board.new).play
    end
    end
    
  
if game.over?
  puts "Would you like to play again? Y/N"
  input = gets.downcase.strip
  if input == "y"
   #go back to line 7
  else exit
end
end


  

#problem 1: how to set up game with human or computer
#problem 2: how to initialize game after player enters first move
#problem 3: how to loop back to line 7 if game is over
  


#You can implement this logic within the bin/tictactoe directly or encapsulate it within Game via a method like #start and simply evoke that method in the CLI. There is no wrong way to implement code that works.